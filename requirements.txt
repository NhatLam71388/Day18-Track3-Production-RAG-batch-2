# Core
pypdf>=4.0
python-dotenv>=1.0
openai>=1.30

# M2 — Hybrid search
underthesea>=6.8
rank-bm25>=0.2.2
qdrant-client>=1.9

# M1/M2/M3 — embeddings + reranker (transformers 5.x ↔ tokenizers 0.22+)
transformers>=4.57
sentence-transformers>=3.3
flashrank>=0.2            # optional alternative reranker

# M4 — RAGAS evaluation (0.4.x API: EvaluationDataset + LangchainLLMWrapper)
ragas>=0.4
datasets>=2.19
langchain-openai>=1.0
langchain-community>=0.4
